Default Run Mode:
Import the external jars found in the external JARs folder, the same way as adding the default external JARs
click run



Run with Debugger:
Run via the Debugger
Step through breakpoints at top of each functionality


NOTE: There are screenshots of the sent emails in the Phase 2 word document. 

If you would like to recieve these emails, change the username when the user is added, and change the username parameter in the driver. 

The JavaDoc output can be found in the index.html file in the doc folder.
